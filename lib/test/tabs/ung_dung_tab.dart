import 'package:flutter/material.dart';

class UngDungTab extends StatelessWidget {
  const UngDungTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ung dung'),
    );
  }
}
